/*-
 * Empty kernel option header for out-of-tree builds of if_bcm4313(4).
 *
 * When building against a kernel build directory (KERNBUILDDIR) the
 * kernel's own opt_wlan.h is used instead; this empty file only serves
 * standalone builds against a plain source tree.
 */
